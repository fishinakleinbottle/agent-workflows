[project]
name = "agent-workflows"
version = "0.1.0"
description = "Central repository for portable AI agent skills, agents, and rules"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "jinja2>=3.1",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
